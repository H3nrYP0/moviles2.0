name: optica_app
description: Aplicación óptica optimizada
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Navegación y estado
  provider: ^6.1.1
  
  # HTTP
  http: ^1.1.0
  
  # Storage
  shared_preferences: ^2.2.2
  
  # Formateo
  intl: 0.20.2  # YA LO TIENES 👍
  mailer: ^6.6.0
  
  # DEPENDENCIA CRÍTICA PARA EL CALENDARIO - AÑADE ESTA LÍNEA
  flutter_localizations:
    sdk: flutter  # Esta viene con el SDK de Flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true