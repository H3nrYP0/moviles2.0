name: optica_app
description: Aplicación óptica optimizada
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Navegación y estado
  provider: ^6.1.1  # Simple, ya lo conoces
  
  # HTTP
  http: ^1.1.0  # Simple, sin complicaciones
  
  # Storage
  shared_preferences: ^2.2.2  # Simple y confiable
  
  # Formateo
  intl: ^0.19.0
  mailer: ^6.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true